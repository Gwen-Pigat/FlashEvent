{% extends '::base.html.twig' %}

{% block body -%}
    <h1>Création d'événement</h1>

    {{ form(form) }}

        <ul class="record_actions">
    <li>
        <a href="{{ path('evenement') }}">
            Retour à la liste
        </a>
    </li>
</ul>
{% endblock %}
